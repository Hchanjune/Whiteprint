spring:
  application:
    name: api-command

  docker:
    compose:
      enabled: false

#  kafka:
#    bootstrap-servers: ""
#
#  datasource:
#    driver-class-name :

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
    endpoint:
      prometheus:
        enabled: true

operation-manager:
  webmvc:
    mdc-service-aspect:
      enabled: true
    micrometer:
      enabled: true
    logging:
      enabled: true
      pretty: false
      json: true
      success-level: INFO
      failure-level: ERROR